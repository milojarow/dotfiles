# Brave browser flags configuration
# Enable Wayland IME support for emoji and text input via wl-ime-type

--enable-wayland-ime
--wayland-text-input-version=3
# Disable GPU process sandbox to reduce NVIDIA/i915 driver crash surface under Wayland.
--disable-gpu-sandbox
