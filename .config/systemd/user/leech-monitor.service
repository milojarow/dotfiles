[Unit]
Description=Leech Monitor — detect CPU/RAM drifting processes
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.scripts/leech-monitor.sh
# Keep logs from the script itself (stdout/stderr go to journal)
StandardOutput=journal
StandardError=journal
