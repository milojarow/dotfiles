# Cap window size on first open so tall verticals (e.g. 1080x1920 shorts) and
# 4K horizontals don't spawn larger than the screen and force manual resize.
# Aspect ratio is preserved; mpv only shrinks if the natural size exceeds the
# given fraction of the output. Floating-placer.service then positions it.
autofit-larger=95%x90%
