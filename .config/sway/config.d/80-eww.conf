# Restart eww via systemd on every sway reload.
# systemd manages the daemon lifecycle (Restart=always keeps it alive if it crashes).
# --no-startup-id: systemctl has no startup notification support, suppresses loading cursor.
exec_always --no-startup-id systemctl --user restart eww.service
